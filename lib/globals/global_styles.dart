import 'dart:ui';

import 'package:flutter/material.dart';

/// This file contains the styles defined for this app, such as border radius,
/// colors, and what more to come ...

Color infoColor = const Color.fromARGB(255, 4, 118, 7);
Color warnColor = const Color.fromARGB(255, 172, 125, 14);

class CiRadius {
  static const Radius r1 = Radius.circular(15);

  static const Radius r2 = Radius.circular(10);

  static const Radius r3 = Radius.circular(5);
}
